% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_descriptive.R
\name{dp_mean}
\alias{dp_mean}
\title{DP mean}
\usage{
dp_mean(
  x,
  epsilon,
  bounds = range(x, na.rm = TRUE),
  mechanism = c("laplace", "gaussian", "analytic_gaussian"),
  delta = NULL
)
}
\arguments{
\item{x}{Numeric vector (NAs ignored).}

\item{epsilon}{Privacy parameter epsilon (> 0).}

\item{bounds}{Bounds c(L, U) assumed known; data are clipped to them.
Defaults to the observed range (use a priori bounds in practice).}

\item{mechanism}{One of \code{"laplace"}, \code{"gaussian"},
\code{"analytic_gaussian"}.}

\item{delta}{Required (in (0,1)) when a Gaussian mechanism is used.}
}
\value{
An object of class \code{dp_estimate}.
}
\description{
Differentially private sample mean of a bounded variable. Data are clipped
to \code{bounds} before the statistic is computed.
}
\examples{
set.seed(1)
dp_mean(rnorm(500), 1.0, bounds = c(-5, 5))$estimate
}

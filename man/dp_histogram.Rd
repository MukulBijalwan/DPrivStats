% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_descriptive2.R
\name{dp_histogram}
\alias{dp_histogram}
\title{DP histogram}
\usage{
dp_histogram(x, epsilon, breaks = NULL, normalize = FALSE)
}
\arguments{
\item{x}{Numeric vector (NAs ignored).}

\item{epsilon}{Privacy parameter epsilon (> 0).}

\item{breaks}{Numeric vector of cut points (monotone). Defaults to deciles
of the observed data.}

\item{normalize}{Logical; return relative frequencies instead of counts.}
}
\value{
An object of class \code{dp_estimate} with element \code{estimate}
  (named numeric vector of privatized bin counts/proportions).
}
\description{
Releases bin counts of a bounded variable using Laplace noise with
sensitivity 1 per bin, followed by non-negativity post-processing and
optional normalization to probabilities.
}
\examples{
set.seed(4)
fit <- dp_histogram(rnorm(500), 1.0, breaks = seq(-3, 3, by = 1))
fit$estimate
}

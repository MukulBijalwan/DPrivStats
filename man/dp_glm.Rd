% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_regression2.R
\name{dp_glm}
\alias{dp_glm}
\title{DP-SGD for generalized linear models}
\usage{
dp_glm(
  formula,
  data,
  family = stats::gaussian(),
  epsilon,
  delta,
  max_grad_norm = 1,
  n_iter = 1000,
  lr = 0.01,
  batch_size = NULL,
  bounds = NULL
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Data frame.}

\item{family}{A GLM family (e.g. \code{\link[stats]{binomial}()}).}

\item{epsilon}{Privacy parameter epsilon (> 0).}

\item{delta}{Privacy parameter delta in (0, 1); required.}

\item{max_grad_norm}{Per-sample gradient clipping norm C (> 0).}

\item{n_iter}{Number of SGD iterations.}

\item{lr}{Learning rate.}

\item{batch_size}{Mini-batch size (default full batch).}

\item{bounds}{Named list: \code{bounds$y} = c(L_y, U_y) for the response
(required); optional \code{bounds$x_norm} giving a priori bound C on row
norms of the design matrix when the design itself is private.}
}
\value{
A list of class \code{"dp_glm"} with \code{coefficients},
  \code{epsilon}, \code{delta}, and convergence details.
}
\description{
Fits a GLM with differentially private stochastic gradient descent:
per-sample gradients are clipped to \code{max_grad_norm}, averaged, and
Gaussian noise is added at each iteration. Privacy is accounted with the
advanced-composition heuristic over \code{n_iter} iterations.
}
\examples{
set.seed(12)
d <- data.frame(x = rnorm(400))
d$y <- rbinom(400, 1, plogis(0.5 * d$x))
fit <- dp_glm(y ~ x, d, binomial(), epsilon = 2.0, delta = 1e-6,
              bounds = list(y = c(0, 1)))
fit$coefficients
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{dp_median}
\alias{dp_median}
\title{DP median via the exponential mechanism}
\usage{
dp_median(x, epsilon, bounds, n_bins = 1000)
}
\arguments{
\item{x}{Numeric vector (may contain NAs, ignored).}

\item{epsilon}{Privacy parameter epsilon (> 0).}

\item{bounds}{Bounds c(L, U) assumed known for the data.}

\item{n_bins}{Number of candidate grid points (default 1000).}
}
\value{
A list of class \code{dp_estimate} with element \code{estimate}.
}
\description{
Releases a differentially private estimate of the median using the
exponential mechanism over a grid of candidate values, with score
\eqn{s(t; x) = -|\#\{i : x_i \le t\}| - n/2|} which has L1
sensitivity 1.
}
\examples{
set.seed(42)
fit <- dp_median(rnorm(200), 1.0, bounds = c(-5, 5))
fit$estimate
}

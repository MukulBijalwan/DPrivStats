% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{compare_utility}
\alias{compare_utility}
\title{Compare utility of non-private and private fits}
\usage{
compare_utility(
  formula,
  data,
  epsilon_grid,
  delta = 1e-06,
  bounds = NULL,
  n_reps = 10
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Data frame.}

\item{epsilon_grid}{Numeric vector of epsilon values to compare.}

\item{delta}{Delta for Gaussian mechanism.}

\item{bounds}{Bounds list passed to \code{\link{dp_lm}}.}

\item{n_reps}{Number of repetitions per epsilon (default 10).}
}
\value{
Data frame with columns \code{epsilon}, \code{rep},
  \code{mse_coef}, \code{max_abs_error}.
}
\description{
Fits \code{\link{dp_lm}} at each epsilon on an epsilon grid and reports MSE
of coefficients relative to the ordinary least squares fit.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(200)); d$y <- 1 + d$x + rnorm(200)
compare_utility(y ~ x, d, c(0.5, 2), 1e-6, list(y = c(-15, 15)),
                n_reps = 3)
}
}

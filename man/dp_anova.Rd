% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_tests2.R
\name{dp_anova}
\alias{dp_anova}
\title{DP one-way ANOVA F-test}
\usage{
dp_anova(formula, data, epsilon, bounds, delta = NULL)
}
\arguments{
\item{formula}{Formula of the form \code{response ~ group}.}

\item{data}{Data frame.}

\item{epsilon}{Total privacy parameter epsilon (> 0).}

\item{bounds}{Common bounds c(L, U) assumed known for both samples.}

\item{delta}{Optional Gaussian-mechanism delta; if NULL Laplace is used.}
}
\value{
An object of class \code{c("dp_htest", "htest")}.
}
\description{
Privatizes group counts, means and variances (equal budget splits) and
computes the one-way ANOVA F statistic from privatized summaries only.
}
\examples{
dat <- data.frame(y = c(rnorm(60), rnorm(60, 1)),
                  g = factor(rep(c("A", "B"), each = 60)))
dp_anova(y ~ g, dat, 2.0, bounds = c(-5, 5))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{clip_gradients}
\alias{clip_gradients}
\title{Per-sample gradient clipping}
\usage{
clip_gradients(grads, max_grad_norm)
}
\arguments{
\item{grads}{Numeric matrix (n x p) of per-sample gradients.}

\item{max_grad_norm}{Positive clipping constant C.}
}
\value{
List with \code{clipped} (the clipped gradients) and
  \code{norms} (original norms).
}
\description{
Clips each row of a gradient matrix to a maximum L2 norm:
\eqn{\tilde g_i = g_i \min(1, C/\|g_i\|_2)}. Used by DP-SGD.
}
\examples{
g <- matrix(rnorm(30), 10, 3)
clip_gradients(g, 1)$norms
}

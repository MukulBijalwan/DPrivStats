% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_tests.R
\name{dp_t_test}
\alias{dp_t_test}
\title{DP two-sample t-test}
\usage{
dp_t_test(
  x,
  y,
  epsilon,
  bounds = c(min(c(x, y), na.rm = TRUE), max(c(x, y), na.rm = TRUE)),
  alternative = c("two.sided", "less", "greater"),
  delta = NULL
)
}
\arguments{
\item{x, y}{Numeric vectors (NAs ignored).}

\item{epsilon}{Total privacy parameter epsilon (> 0).}

\item{bounds}{Common bounds c(L, U) assumed known for both samples.}

\item{alternative}{Character, \code{"two.sided"}, \code{"less"} or
\code{"greater"}.}

\item{delta}{Optional Gaussian-mechanism delta; if NULL Laplace is used.}
}
\value{
An object of class \code{c("dp_htest", "htest")} with elements
  \code{statistic}, \code{estimate}, \code{std.error}, \code{p.value},
  \code{alternative}, and \code{epsilon}.
}
\description{
Differentially private two-sample t-test. The privacy budget is split
evenly across the DP group counts, means and variances; the test statistic
is computed entirely by post-processing of privatized quantities.
}
\examples{
set.seed(7)
dp_t_test(rnorm(200), rnorm(200, 0.5), 1.0, bounds = c(-5, 5))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_tests2.R
\name{dp_ks_test}
\alias{dp_ks_test}
\title{DP Kolmogorov-Smirnov test}
\usage{
dp_ks_test(
  x,
  y,
  epsilon,
  bounds = c(min(c(x, y), na.rm = TRUE), max(c(x, y), na.rm = TRUE)),
  n_grid = 100
)
}
\arguments{
\item{x, y}{Numeric vectors (NAs ignored).}

\item{epsilon}{Total privacy parameter epsilon (> 0).}

\item{bounds}{Common bounds c(L, U) assumed known for both samples.}

\item{n_grid}{Number of grid points for ECDF evaluation (default 100).}
}
\value{
An object of class \code{c("dp_htest", "htest")}.
}
\description{
Releases the ECDFs of two bounded samples on a common grid under DP
(ECDF sensitivity \eqn{1/n} per sample; budget split evenly between the two
samples) and computes the KS statistic from the privatized ECDFs.
}
\examples{
set.seed(8)
dp_ks_test(rnorm(150), rnorm(150, 1), 1.0, bounds = c(-6, 6))
}
